--- ADDRESS ---
localhost:50052

--- ENDPOINT ---
monitoring.IoTMonitoringService/BulkUpdateDevices

--- REQUEST ---
{
  "device_id": "device_001",
  "operation": "update_config",
  "parameters": {
    "update_interval": "45s",
    "alert_threshold": "26.0"
  }
}

--- REQUEST ---
{
  "device_id": "device_002",
  "operation": "restart",
  "parameters": {
    "force": "true"
  }
}

--- REQUEST ---
{
  "device_id": "device_003",
  "operation": "firmware_update",
  "parameters": {
    "version": "2.1.0",
    "rollback_on_failure": "true"
  }
}

--- ASSERTS ---
.total_processed == 3
.successful >= 2
.failed <= 1
.errors | length <= 1
.successful_devices | length >= 2

--- OPTIONS ---
timeout: 20
